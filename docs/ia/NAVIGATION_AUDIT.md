# Navigation audit

The rendered graph contains 32130 deduplicated typed edges. 64 routes are deeper than four navigation edges or unreachable in the navigation-only graph; 15 are important roles. 10 labels point to multiple destinations. These counts describe structure, not automatically defective editorial choices.
