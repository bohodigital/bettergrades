# Unit 2A release record

Status: pre-release candidate.

This file is completed only after all of the following exist:

- exact reviewed commit and tree;
- owner-only Sites version with one allowed owner and zero groups;
- browser QA of that exact candidate;
- GitHub PR and required checks;
- merged `main` commit and tree;
- Cloudflare Pages immutable deployment URL;
- pages.dev, apex, and www verification;
- route, visual, answer-key, navigation, metadata, analytics, sitemap, robots, headers, cache, and noindex evidence;
- tested rollback target.

The owner program supplies explicit authorization to publish after these gates pass. No separate DNS, binding, ownership, billing, or credential change is authorized or required.
