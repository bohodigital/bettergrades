# Unit 2A postmortem

The canonical production postmortem for Unit 2A is maintained in [`../units-2a-2b/UNIT_2A_POSTMORTEM.md`](../units-2a-2b/UNIT_2A_POSTMORTEM.md). The machine-readable companion is [`postmortem.json`](./postmortem.json).

The release was accepted at merged commit `36e0091b9014ea13c7a043ff258c9edd05bdf2f4`, tree `839f910b25782065d713c4dcb472a5cf427217b7`, and immutable Cloudflare deployment `https://b9fa0b41.bettergrades-vhc.pages.dev` after the first private visual candidate was rejected and corrected.

The controlling Unit 2B lessons are: validate pedagogical fidelity rather than schema shape alone; require a complete application-model template; render a 34-item visual contact grid before Sites; keep answers server-only and attempt-gated; run cheap fail-closed checks before serial, process-isolated ARM SSR; and wait for semantic hydration readiness before browser measurements.
