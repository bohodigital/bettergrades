import routeIndex from "../../content/algebra/routes.public.json" with { type: "json" };

export const algebraCourseRoutes = Object.freeze(routeIndex.routes);
export const supersededAlgebraPaths = Object.freeze([]);
